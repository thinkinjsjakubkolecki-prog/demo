/**
 * Placeholder — menu-editor w frameworku nie importuje menu z apki.
 * Menu jest dostarczane przez Input binding z page config.
 * Ten plik istnieje żeby import się nie wysypał podczas kompilacji.
 */
export const menu: readonly unknown[] = [];
